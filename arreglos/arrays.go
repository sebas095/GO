package main;

import "fmt";

func main() {
  // Declarar arreglo
  // var array[10] int;

  // Definir arreglo con valores por default
  // array := [4] int{1, 2, 3};
  // array[3] = 20;

  // fmt.Println(len(array));
  // fmt.Println(array);
  // for i := 0; i < len(array); i++ {
  //  fmt.Println(array[i]);
  // }

  var matriz[3][2]int;
  fmt.Println(matriz);
}
